# Security policy

Do not submit credentials, private collection data, certificates, response bodies, or diagnostic
bundles in public issues. Report security defects privately to the future security contact listed
by the project repository. Until that channel exists, do not deploy this alpha checkpoint as a
security-sensitive production client.

The current security guarantees and explicit gaps are documented in `docs/security-model.md` and
`docs/feature-matrix.md`.

# apex-ui

Native GPUI presentation layer for ApexAPI.

The crate contains the compiled application shell, Dock panels, editors, virtualized collection
tree, request/response presentation, command actions, worker-to-UI execution bridge, workspace
opening, atomic document editing, and pure tab/session models. It depends on domain/runner/workspace
crates but protocol crates never depend on GPUI.
